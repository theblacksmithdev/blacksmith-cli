/**
 * {{Name}} Mutation Hooks
 *
 * Create, update, and delete with cache invalidation and error parsing.
 * Generated by Blacksmith. You own this file — customize as needed.
 */

import { useApiMutation } from '@/shared/hooks/use-api-mutation'
import {
  {{names}}CreateMutation,
  {{names}}UpdateMutation,
  {{names}}DestroyMutation,
  {{names}}ListQueryKey,
  {{names}}RetrieveQueryKey,
} from '@/api/generated/@tanstack/react-query.gen'

export function useCreate{{Name}}() {
  return useApiMutation({
    ...{{names}}CreateMutation(),
    invalidateKeys: [{{names}}ListQueryKey()],
  })
}

export function useUpdate{{Name}}(id: string) {
  return useApiMutation({
    ...{{names}}UpdateMutation(),
    invalidateKeys: [
      {{names}}ListQueryKey(),
      {{names}}RetrieveQueryKey({ path: { id } }),
    ],
  })
}

export function useDelete{{Name}}() {
  return useApiMutation({
    ...{{names}}DestroyMutation(),
    invalidateKeys: [{{names}}ListQueryKey()],
  })
}
