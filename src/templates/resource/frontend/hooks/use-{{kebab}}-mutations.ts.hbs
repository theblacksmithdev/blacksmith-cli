/**
 * {{Name}} Mutation Hooks
 *
 * Create, update, and delete with cache invalidation.
 * Generated by Blacksmith. You own this file — customize as needed.
 */

import { useMutation, useQueryClient } from '@tanstack/react-query'
import {
  {{snakes}}CreateMutation,
  {{snakes}}UpdateMutation,
  {{snakes}}DestroyMutation,
  {{snakes}}ListQueryKey,
  {{snakes}}RetrieveQueryKey,
} from '@/api/generated/@tanstack/react-query.gen'

export function useCreate{{Name}}() {
  const qc = useQueryClient()

  return useMutation({
    ...{{snakes}}CreateMutation(),
    onSuccess: () => {
      qc.invalidateQueries({ queryKey: {{snakes}}ListQueryKey() })
    },
  })
}

export function useUpdate{{Name}}() {
  const qc = useQueryClient()

  return useMutation({
    ...{{snakes}}UpdateMutation(),
    onSuccess: (_data: any, variables: any) => {
      qc.invalidateQueries({ queryKey: {{snakes}}ListQueryKey() })
      qc.invalidateQueries({
        queryKey: {{snakes}}RetrieveQueryKey({ path: { id: variables.path.id } }),
      })
    },
  })
}

export function useDelete{{Name}}() {
  const qc = useQueryClient()

  return useMutation({
    ...{{snakes}}DestroyMutation(),
    onSuccess: () => {
      qc.invalidateQueries({ queryKey: {{snakes}}ListQueryKey() })
    },
  })
}
