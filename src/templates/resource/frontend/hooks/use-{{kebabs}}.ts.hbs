/**
 * use{{Names}} Hook
 *
 * Wraps the generated list query with business logic.
 * Generated by Forge. You own this file — customize as needed.
 */

import { useQuery } from '@tanstack/react-query'
import {
  {{snakes}}ListOptions,
} from '@/api/generated/@tanstack/react-query.gen'

interface Use{{Names}}Params {
  page?: number
  search?: string
  ordering?: string
}

export function use{{Names}}(params: Use{{Names}}Params = {}) {
  return useQuery({
    ...{{snakes}}ListOptions({
      query: {
        page: params.page ?? 1,
        search: params.search,
        ordering: params.ordering ?? '-created_at',
      },
    }),
    select: (data: any) => ({
      {{names}}: data.results ?? [],
      total: data.count ?? 0,
      hasNext: !!data.next,
      hasPrev: !!data.previous,
    }),
  })
}
