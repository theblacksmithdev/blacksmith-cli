/**
 * {{Names}} List Page
 *
 * Lists all {{names}} with loading states.
 * Generated by Forge. You own this file — customize as needed.
 */

import { use{{Names}} } from '../hooks/use-{{kebabs}}'
import { {{Name}}List } from '../components/{{kebab}}-list'
import type { QueryClient } from '@tanstack/react-query'
import {
  {{snakes}}ListOptions,
} from '@/api/generated/@tanstack/react-query.gen'

/**
 * Prefetch data before the page mounts (used by Forge router as a loader).
 */
export function prefetch(queryClient: QueryClient) {
  return queryClient.ensureQueryData(
    {{snakes}}ListOptions({ query: { page: 1 } })
  )
}

export default function {{Names}}Page() {
  const { data, isLoading } = use{{Names}}()

  return (
    <div>
      <div className="flex items-center justify-between mb-6">
        <h1 className="text-2xl font-bold text-gray-900">{{Names}}</h1>
      </div>

      <{{Name}}List
        {{names}}={data?.{{names}} ?? []}
        isLoading={isLoading}
      />
    </div>
  )
}
