/**
 * Auth Hook
 *
 * Re-exports useAuth from @flatui/auth-ui for convenience.
 * Import from here so your app has a single auth import path.
 *
 * Generated by Forge. You own this file — customize as needed.
 */

import { useAuth as useFlatUIAuth } from '@flatui/auth-ui'

export function useAuth() {
  const auth = useFlatUIAuth()

  return {
    user: auth.user,
    isLoading: auth.loading,
    isAuthenticated: !!auth.user,
    error: auth.error,
    login: auth.signInWithEmail,
    register: auth.signUpWithEmail,
    logout: auth.signOut,
    sendPasswordResetEmail: auth.sendPasswordResetEmail,
    confirmPasswordReset: auth.confirmPasswordReset,
    socialProviders: auth.socialProviders,
  }
}
