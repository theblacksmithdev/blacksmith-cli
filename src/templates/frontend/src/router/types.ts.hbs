/**
 * Blacksmith Router Type Definitions
 *
 * These types define the route configuration format.
 * Generated by Blacksmith. You own this file — customize as needed.
 */

export interface BlacksmithRoute {
  /** URL path segment */
  path?: string
  /** Mark as index route (matches parent path exactly) */
  index?: boolean
  /**
   * Page component to render, resolved by convention:
   * - 'home' → src/pages/home.tsx
   * - 'posts/posts-page' → src/features/posts/pages/posts-page.tsx
   */
  page?: string
  /** Requires authentication to access */
  auth?: boolean
  /** Role-based access control (user must have at least one) */
  roles?: string[]
  /** Nested child routes */
  children?: BlacksmithRoute[]
}

export interface BlacksmithAuthConfig {
  /** Login page path (default: '/login') */
  login: string
  /** Registration page path (default: '/register') */
  register: string
  /** Forgot password page path */
  forgotPassword: string
  /** Reset password page path (must include :token param) */
  resetPassword: string
  /** Redirect path after successful login */
  afterLogin: string
  /** Redirect path after logout */
  afterLogout: string
}

export interface BlacksmithRoutes {
  /** Authentication route configuration */
  auth: BlacksmithAuthConfig
  /** Application routes */
  routes: BlacksmithRoute[]
}
