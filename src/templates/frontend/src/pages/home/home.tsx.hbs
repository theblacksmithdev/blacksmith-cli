import {
  Card,
  CardHeader,
  CardTitle,
  CardDescription,
  CardContent,
  CardFooter,
  Button,
  Badge,
  Separator,
  Alert,
  AlertTitle,
  AlertDescription,
  TabsPrimitives,
  TabsList,
  TabsTrigger,
  TabsContent,
  Tooltip,
} from '@blacksmith-ui/react'
import {
  ExternalLink,
  Zap,
  Shield,
  Code2,
  Database,
  Layout,
  Rocket,
  Terminal,
  BookOpen,
  Anvil,
  Copy,
  Check,
} from 'lucide-react'
import { Link } from 'react-router-dom'
import { useAuth } from '@/features/auth/hooks/use-auth'
import { useCopyToClipboard } from '@blacksmith-ui/hooks'

const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:8000'

const features = [
  {
    icon: Database,
    title: 'Django REST API',
    description: 'Production-ready backend with JWT auth, serializers, and OpenAPI docs.',
    badge: 'Backend',
  },
  {
    icon: Layout,
    title: 'React + TypeScript',
    description: 'Vite-powered frontend with TanStack Query, React Router, and Tailwind CSS.',
    badge: 'Frontend',
  },
  {
    icon: Shield,
    title: 'Authentication',
    description: 'Complete auth flow with login, register, password reset, and role guards.',
    badge: 'Full Stack',
  },
  {
    icon: Code2,
    title: 'Type-Safe API',
    description: 'Auto-generated TypeScript client from your OpenAPI schema via blacksmith sync.',
    badge: 'DX',
  },
  {
    icon: Zap,
    title: 'Blacksmith UI',
    description: '80+ accessible components, form library, hooks, and dark mode theming.',
    badge: 'UI',
  },
  {
    icon: Rocket,
    title: 'Developer Experience',
    description: 'Hot reload, AI-ready CLAUDE.md, and one command to run the full stack.',
    badge: 'DX',
  },
]

const steps = [
  {
    command: 'blacksmith dev',
    title: 'Start the dev server',
    description: 'Runs Django, Vite, and OpenAPI watcher concurrently.',
  },
  {
    command: 'blacksmith make:resource Post',
    title: 'Scaffold a resource',
    description: 'Generates model, serializer, viewset, hooks, and pages.',
  },
  {
    command: 'blacksmith sync',
    title: 'Sync API types',
    description: 'Regenerates the TypeScript client from your Django schema.',
  },
]

function CommandBlock({ command }: { command: string }) {
  const { status, copy } = useCopyToClipboard(2000)
  const copied = status === 'copied'

  return (
    <div className="flex items-center gap-2 rounded-lg bg-muted px-3 py-2 font-mono text-sm">
      <Terminal className="h-3.5 w-3.5 text-muted-foreground shrink-0" />
      <code className="flex-1">{command}</code>
      <Tooltip content={copied ? 'Copied!' : 'Copy command'}>
        <Button variant="ghost" size="icon" className="h-7 w-7" onClick={() => copy(command)}>
          {copied ? (
            <Check className="h-3.5 w-3.5 text-green-500" />
          ) : (
            <Copy className="h-3.5 w-3.5" />
          )}
        </Button>
      </Tooltip>
    </div>
  )
}

export default function HomePage() {
  const { isAuthenticated, user } = useAuth()

  return (
    <div className="space-y-12">
      {/* Hero */}
      <section className="text-center space-y-6 pt-10 pb-2">
        <div className="flex justify-center">
          <Badge variant="secondary" className="gap-1.5 px-3 py-1 text-sm">
            <Anvil className="h-3.5 w-3.5" />
            Scaffolded with Blacksmith
          </Badge>
        </div>
        <h1 className="text-4xl font-bold tracking-tight sm:text-5xl lg:text-6xl">
          Welcome to{' '}
          <span className="text-primary">{{projectName}}</span>
        </h1>
        <p className="text-lg text-muted-foreground max-w-2xl mx-auto leading-relaxed">
          Your fullstack Django + React application is ready to go.
          {isAuthenticated
            ? ` Signed in as ${user?.displayName || user?.email}.`
            : ' Sign in to start building.'}
        </p>
        <div className="flex justify-center gap-3 pt-2">
          {isAuthenticated ? (
            <Button size="lg">
              <Rocket className="mr-2 h-4 w-4" />
              Get Started
            </Button>
          ) : (
            <Button size="lg" asChild>
              <Link to="/login">
                <Rocket className="mr-2 h-4 w-4" />
                Sign In
              </Link>
            </Button>
          )}
          <Button variant="outline" size="lg" asChild>
            <a href={`${API_URL}/api/docs/`} target="_blank" rel="noopener noreferrer">
              <BookOpen className="mr-2 h-4 w-4" />
              API Docs
            </a>
          </Button>
        </div>
      </section>

      <Separator />

      {/* Getting Started */}
      <section className="space-y-6">
        <div className="text-center space-y-2">
          <h2 className="text-2xl font-semibold tracking-tight">Get Started in 3 Steps</h2>
          <p className="text-muted-foreground">
            Everything you need to go from scaffold to production.
          </p>
        </div>
        <div className="grid gap-4 md:grid-cols-3">
          {steps.map((step, index) => (
            <Card key={step.command}>
              <CardHeader className="pb-3">
                <div className="flex items-center gap-3">
                  <div className="flex h-8 w-8 shrink-0 items-center justify-center rounded-full bg-primary text-primary-foreground text-sm font-semibold">
                    {index + 1}
                  </div>
                  <CardTitle className="text-base">{step.title}</CardTitle>
                </div>
              </CardHeader>
              <CardContent className="space-y-3">
                <CardDescription>{step.description}</CardDescription>
                <CommandBlock command={step.command} />
              </CardContent>
            </Card>
          ))}
        </div>
      </section>

      {/* Features */}
      <section className="space-y-6">
        <div className="text-center space-y-2">
          <h2 className="text-2xl font-semibold tracking-tight">What's Included</h2>
          <p className="text-muted-foreground">
            A batteries-included stack so you can focus on your product.
          </p>
        </div>
        <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
          {features.map((feature) => (
            <Card key={feature.title} className="group transition-colors hover:border-primary/50">
              <CardHeader className="pb-3">
                <div className="flex items-start justify-between">
                  <div className="flex h-10 w-10 items-center justify-center rounded-lg bg-primary/10 transition-colors group-hover:bg-primary/20">
                    <feature.icon className="h-5 w-5 text-primary" />
                  </div>
                  <Badge variant="outline" className="text-xs">
                    {feature.badge}
                  </Badge>
                </div>
              </CardHeader>
              <CardContent className="space-y-1.5">
                <CardTitle className="text-base">{feature.title}</CardTitle>
                <CardDescription>{feature.description}</CardDescription>
              </CardContent>
            </Card>
          ))}
        </div>
      </section>

      {/* Resources */}
      <section>
        <Card>
          <CardHeader>
            <CardTitle>Resources</CardTitle>
            <CardDescription>
              Documentation, tools, and links to help you build.
            </CardDescription>
          </CardHeader>
          <CardContent>
            <TabsPrimitives.Root defaultValue="docs">
              <TabsList>
                <TabsTrigger value="docs">Documentation</TabsTrigger>
                <TabsTrigger value="tools">Dev Tools</TabsTrigger>
              </TabsList>
              <TabsContent value="docs" className="pt-4">
                <div className="flex flex-wrap gap-3">
                  <Button variant="outline" size="sm" asChild>
                    <a href={`${API_URL}/api/docs/`} target="_blank" rel="noopener noreferrer">
                      <ExternalLink className="mr-2 h-3.5 w-3.5" />
                      Swagger UI
                    </a>
                  </Button>
                  <Button variant="outline" size="sm" asChild>
                    <a href={`${API_URL}/api/redoc/`} target="_blank" rel="noopener noreferrer">
                      <ExternalLink className="mr-2 h-3.5 w-3.5" />
                      ReDoc
                    </a>
                  </Button>
                  <Button variant="outline" size="sm" asChild>
                    <a href={`${API_URL}/api/schema/`} target="_blank" rel="noopener noreferrer">
                      <ExternalLink className="mr-2 h-3.5 w-3.5" />
                      OpenAPI Schema
                    </a>
                  </Button>
                </div>
              </TabsContent>
              <TabsContent value="tools" className="pt-4">
                <div className="flex flex-wrap gap-3">
                  <Button variant="outline" size="sm" asChild>
                    <a href={`${API_URL}/admin/`} target="_blank" rel="noopener noreferrer">
                      <ExternalLink className="mr-2 h-3.5 w-3.5" />
                      Django Admin
                    </a>
                  </Button>
                </div>
              </TabsContent>
            </TabsPrimitives.Root>
          </CardContent>
          <Separator />
          <CardFooter className="pt-4">
            <Alert className="w-full">
              <Terminal className="h-4 w-4" />
              <AlertTitle>Quick Tip</AlertTitle>
              <AlertDescription>
                Run <code className="rounded bg-muted px-1.5 py-0.5 font-mono text-xs">blacksmith make:resource Post</code> to
                scaffold a full CRUD resource across backend and frontend in seconds.
              </AlertDescription>
            </Alert>
          </CardFooter>
        </Card>
      </section>
    </div>
  )
}
