// Prisma schema for {{projectName}}.
// Generated by Blacksmith. You own this file — customize as needed.
//
// Field names are snake_case on purpose: they are serialized straight to JSON,
// and the frontend client is generated from that JSON.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model User {
  id          Int      @id @default(autoincrement())
  email       String   @unique
  password    String
  first_name  String
  last_name   String   @default("")
  is_active   Boolean  @default(true)
  is_staff    Boolean  @default(false)
  date_joined DateTime @default(now())
  updated_at  DateTime @updatedAt

  // Back-relations for resources are added here by `blacksmith make:resource`
  // blacksmith:user-relations

  @@map("users")
}

// Resource models are appended here by `blacksmith make:resource`
// blacksmith:models
